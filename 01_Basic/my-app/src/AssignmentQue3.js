// 5 Create a coponent na6ed "Header" that takes one prop - "title". The co6ponent should display a header
// ele6ent with the gi2en title5

function Header(props) {
    return(
        <>
          <h1>{props.title}</h1>
        </>
    )
}

export{Header}
